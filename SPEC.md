# KoNomad 개선 실행 계획

## 실행 순서 요약

| Phase | 핵심 작업 | 의존성 | 상태 |
|---|---|---|---|
| 1 | 네비게이션 정리 | 없음 | ✅ |
| 2 | 필터바 개편 | 없음 | ✅ |
| 3 | Mock 데이터 + 카드 구조 변경 | Phase 2 | ⬜ |
| 4 | 좋아요/싫어요 인터랙션 + 정렬 | Phase 3 | ⬜ |
| 5 | 홈페이지 섹션 정리 | 없음 | ✅ |
| 6 | 도시 카드 링크 + 상세 페이지 레이아웃 | 없음 | ✅ |
| 7 | 도시 상세 페이지 리뷰 섹션 | Phase 6 | ✅ |
| 8 | 도시 카드 좋아요/싫어요 버튼 레이아웃 변경 | 없음 | ✅ |
| 9 | 필터바 Select 전체 너비 + 라벨 텍스트 추가 | 없음 | ⬜ |

---

## [x] Phase 1: 불필요한 페이지/라우트 삭제 및 네비게이션 정리

### 오버뷰

홈페이지(`/`)와 인증 페이지(`/login`, `/register`, `/auth/confirm`) 외 모든 불필요한 요소를 제거하고, 네비게이션 바에서 존재하지 않는 페이지로의 링크를 삭제한다. 인증 및 프로필 관련 버튼만 남긴다.

### 수정/개선 항목

- [x] `navbar.tsx` — 데스크톱 메뉴에서 "홈", "도시목록", "지도", "밋업", "채팅" 링크 모두 삭제. "로그인", "회원가입" 버튼만 유지
- [x] `navbar.tsx` — 모바일 햄버거 메뉴에서도 동일하게 페이지 이동 링크 삭제, 인증 버튼만 유지
- [x] `navbar.tsx` — 도시 검색 input 삭제 (필터바에 검색 기능이 이미 있음)

### 검증 항목

- [x] 네비게이션 바에 "로그인", "회원가입" 버튼만 표시되는지 확인
- [x] 모바일 메뉴에서도 인증 버튼만 표시되는지 확인
- [x] 로고 클릭 시 홈(`/`)으로 이동하는지 확인
- [x] 빌드(`npm run build`) 성공 확인

---

## [x] Phase 2: 필터바 개편

### 오버뷰

기존 필터바의 드롭다운(지역, 생활비, 태그, 인터넷)과 정렬/뷰 토글을 제거하고, 새로운 필터 기준(예산, 지역, 환경, 최고 계절)으로 교체한다. 필터 선택 시 도시 목록이 필터링되어야 한다.

### 수정/개선 항목

- [x] `filter-bar.tsx` — 기존 드롭다운(지역, 생활비, 태그, 인터넷) 모두 제거
- [x] `filter-bar.tsx` — 정렬 드롭다운 제거
- [x] `filter-bar.tsx` — 뷰 토글 버튼(그리드, 지도, 차트, 비교) 제거
- [x] `filter-bar.tsx` — 새 필터 드롭다운 4개 추가:
  - **예산**: 100만원 이하, 100~200만원, 200만원 이상
  - **지역**: 전체, 수도권, 경상도, 전라도, 강원도, 제주도, 충청도
  - **환경**: 자연친화, 도심선호, 카페작업, 코워킹 필수
  - **최고 계절**: 봄, 여름, 가을, 겨울
- [x] `filter-bar.tsx` — 필터 상태를 상위 컴포넌트에 전달하는 콜백 props 추가
- [x] `page.tsx` (홈페이지) — 필터 상태를 관리하고, 선택된 필터에 따라 `cities` 배열을 필터링하여 `CityGrid`에 전달하는 로직 추가

### 검증 항목

- [x] 4개 필터 드롭다운이 정상 렌더링되는지 확인
- [x] 각 필터 선택 시 도시 카드 목록이 정확히 필터링되는지 확인
- [x] 필터 복수 조합 시 교차 필터링(AND 조건)이 동작하는지 확인
- [x] 필터 해제 시 전체 목록으로 복원되는지 확인
- [x] 모바일/태블릿에서 필터바 레이아웃이 깨지지 않는지 확인
- [x] 빌드 성공 확인

---

## [x] Phase 3: Mock 데이터 수정 및 카드 구조 변경

### 오버뷰

기존 `mock-data.ts`의 `City` 인터페이스에 새 필터 필드(budget, area, environment, best_season)를 추가하고, 12개 도시 데이터에 각각 적절한 값을 배정한다. 도시 카드 UI에서 별점(⭐ nomad_score)을 제거하고, 새 필터 필드를 key-value 형태로 표시한다. "자세히 보기" 버튼이 있다면 삭제한다.

### 수정/개선 항목

- [x] `mock-data.ts` — `City` 인터페이스에 새 필드 추가:
  - `budget`: `"100만원 이하"` | `"100~200만원"` | `"200만원 이상"`
  - `area`: `"수도권"` | `"경상도"` | `"전라도"` | `"강원도"` | `"제주도"` | `"충청도"`
  - `environment`: `string[]` (예: `["도심선호", "카페작업"]`)
  - `best_season`: `string[]` (예: `["봄", "가을"]`)
- [x] `mock-data.ts` — 12개 도시 데이터에 새 필드 값 배정. 각 도시의 `monthly_cost`에 맞는 `budget` 값, 실제 지역에 맞는 `area` 값, 도시 특성에 맞는 `environment`와 `best_season` 값 설정
- [x] `city-card.tsx` — 카드 하단의 "⭐ {nomad_score} 리뷰 {review_count}개" 영역 제거
- [x] `city-card.tsx` — 카드에 key-value 정보 영역 추가: 예산, 지역, 환경, 최고 계절을 라벨-값 형태로 표시
- [x] `city-card.tsx` — "자세히 보기" 버튼이 있다면 삭제

### 검증 항목

- [x] 모든 12개 도시에 budget, area, environment, best_season 값이 존재하는지 확인
- [x] 카드에 별점(⭐)이 더 이상 표시되지 않는지 확인
- [x] 카드에 예산/지역/환경/최고 계절이 key-value로 정확히 표시되는지 확인
- [x] Phase 2의 필터가 새 필드 기준으로 정상 동작하는지 확인
- [ ] 카드 레이아웃이 모바일/태블릿/데스크톱에서 깨지지 않는지 확인
- [x] 빌드 성공 확인

---

## [x] Phase 4: 좋아요/싫어요 인터랙션 및 정렬

### 오버뷰

도시 카드의 좋아요(👍)/싫어요(👎) 버튼에 클릭 인터랙션을 추가한다. 클릭 시 아이콘 색상이 변경되고 카운트가 증감한다. "TOP 추천 도시" 제목을 "도시 리스트"로 변경하고, 도시 목록을 좋아요 수 내림차순으로 정렬한다.

### 수정/개선 항목

- [x] `city-card.tsx` — 👍/👎 버튼을 클릭 가능한 인터랙티브 버튼으로 변경
- [x] `city-card.tsx` — 좋아요 클릭 시: 아이콘 색상 파란색으로 변경, like_count +1. 이미 좋아요 상태면 해제하고 like_count -1. 싫어요 상태였다면 싫어요 해제 후 좋아요 적용
- [x] `city-card.tsx` — 싫어요 클릭 시: 아이콘 색상 빨간색으로 변경, dislike_count +1. 이미 싫어요 상태면 해제하고 dislike_count -1. 좋아요 상태였다면 좋아요 해제 후 싫어요 적용
- [x] `city-grid.tsx` — 섹션 제목 "TOP 추천 도시"를 "도시 리스트"로 변경
- [x] `city-grid.tsx` — 도시 목록을 `like_count` 내림차순으로 정렬하여 표시
- [x] 좋아요/싫어요 상태 관리 (컴포넌트 로컬 state 또는 Zustand store — DB 미사용이므로 클라이언트 state로 관리)

### 검증 항목

- [x] 👍 클릭 시 아이콘이 파란색으로 변하고 숫자가 +1 되는지 확인
- [x] 👍 재클릭 시 색상 해제되고 숫자가 -1 되는지 확인
- [x] 👎 클릭 시 아이콘이 빨간색으로 변하고 숫자가 +1 되는지 확인
- [x] 좋아요 상태에서 👎 클릭 시 좋아요가 해제되고 싫어요가 적용되는지 확인 (반대도 동일)
- [x] 섹션 제목이 "도시 리스트"로 표시되는지 확인
- [x] 도시가 좋아요 수 기준 내림차순으로 정렬되어 있는지 확인
- [x] 빌드 성공 확인

---

## [x] Phase 5: 홈페이지 섹션 정리

### 오버뷰

홈페이지에서 현재 단계에서 불필요한 섹션들을 제거하여 페이지를 간결하게 만든다. 핵심 콘텐츠(네비게이션, 히어로, 필터, 도시 그리드)만 남기고, 부가적인 위젯과 프로모션 섹션을 정리한다.

### 수정/개선 항목

- [x] `page.tsx` — 아래 컴포넌트들을 홈페이지에서 제거:
  - [x] `SocialProofBar` (언론사 로고)
  - [x] `PressQuotes` (언론 인용문)
  - [x] `Sidebar` (밋업, 광고, 여행중 멤버, 신규 멤버, 채팅 위젯)
  - [x] `StatsSection` (통계 수치)
  - [x] `PopularTags` (인기 태그)
  - [x] `RecentReviews` (최근 리뷰)
  - [x] `BottomCta` (하단 CTA)
  - [x] `FloatingCtaBar` (플로팅 CTA 바)
  - [x] `SettingsToggle` (설정 토글)
- [x] `page.tsx` — 메인 콘텐츠 레이아웃을 사이드바 없이 전체 폭으로 변경 (3+1 컬럼 → 3컬럼 풀 폭)
- [x] 제거된 컴포넌트 파일들은 삭제하지 않고 홈페이지 import만 제거 (향후 Phase에서 재사용 가능)

### 검증 항목

- [x] 홈페이지에 Navbar, Hero, FilterBar, CityGrid, Footer만 남아있는지 확인
- [x] 사이드바 제거 후 도시 그리드가 전체 폭으로 표시되는지 확인
- [x] 모바일/태블릿/데스크톱 레이아웃이 정상인지 확인
- [x] 제거한 섹션들의 컴포넌트 파일이 삭제되지 않고 남아있는지 확인
- [x] 빌드 성공 확인

---

## [x] Phase 6: 도시 카드 링크 연결 + 상세 페이지 레이아웃

### 오버뷰

도시 카드 클릭 시 이동할 상세 페이지(`/city/[id]`)의 기본 라우트와 레이아웃을 생성한다. CityCard를 Next.js Link로 감싸고, 상세 페이지에 히어로 + 핵심 지표를 표시한다. 기존 Navbar/Footer를 재사용한다.

### 수정/개선 항목

- [ ] `app/src/components/sections/city-card.tsx` — 카드 최외곽 `<div>`를 Next.js `Link`로 변경하여 `/city/{city.id}`로 이동
  - [ ] 기존 좋아요/싫어요 버튼의 `stopPropagation`은 이미 구현되어 있으므로 Link 이동과 충돌 없음
- [ ] `app/src/app/city/[id]/page.tsx` — 도시 상세 페이지 신규 생성:
  - [ ] `cities` 배열에서 `id`로 도시 검색, 없으면 `notFound()` 호출
  - [ ] 히어로 영역: 도시 이미지 배경 + 도시명(한/영) + 순위 뱃지 + 태그
  - [ ] 핵심 지표 그리드 (카드 형태): 월 생활비(₩), 인터넷 속도(Mbps), 안전 점수, 현재 기온/날씨, AQI
  - [ ] 상세 정보 섹션: 예산, 지역, 환경(태그), 최고 계절(태그)
  - [ ] 좋아요/싫어요 카운트 표시
  - [ ] Navbar + Footer 포함 (기존 컴포넌트 재사용)
  - [ ] "← 홈으로 돌아가기" 링크 (Next.js Link to "/")

### 검증 항목

- [ ] `/city/1` 접속 시 서울 상세 페이지가 표시되는지 확인
- [ ] 존재하지 않는 ID(`/city/999`) 접속 시 404 페이지가 표시되는지 확인
- [ ] 홈페이지에서 도시 카드 클릭 시 상세 페이지로 이동하는지 확인
- [ ] 좋아요/싫어요 버튼 클릭 시 상세 페이지로 이동하지 않는지 확인
- [ ] 상세 페이지에서 "홈으로 돌아가기" 클릭 시 홈으로 이동하는지 확인
- [ ] 핵심 지표(비용, 인터넷, 안전, 기온, AQI)가 정확히 표시되는지 확인
- [ ] 모바일/데스크톱 레이아웃이 정상인지 확인
- [ ] `npm run lint && npx tsc --noEmit` 통과

---

## [x] Phase 7: 도시 상세 페이지 리뷰 섹션

### 오버뷰

도시 상세 페이지에 해당 도시의 리뷰 목록을 표시하는 섹션을 추가한다. `mock-data.ts`의 `reviews` 배열에서 `city_name`이 일치하는 리뷰를 필터링하여 보여준다. 현재 서울/제주/부산 3개 도시에 리뷰 데이터가 존재한다.

### 수정/개선 항목

- [ ] `app/src/components/sections/city-detail-reviews.tsx` — 도시 리뷰 섹션 컴포넌트 신규 생성:
  - [ ] props로 `cityName: string` 받기
  - [ ] `reviews` 배열에서 `city_name === cityName`인 리뷰만 필터링
  - [ ] 리뷰 카드: 유저 아바타(원형 배경 + 이니셜), 이름, 평점(⭐), 체류기간, 본문, 장점/단점 태그, 좋아요 수
  - [ ] 리뷰가 없을 경우 "아직 리뷰가 없습니다" 빈 상태 메시지 표시
- [ ] `app/src/app/city/[id]/page.tsx` — 리뷰 섹션 컴포넌트 추가
  - [ ] `city-detail-reviews` import 및 `cityName={city.name_ko}` props 전달

### 검증 항목

- [ ] `/city/1` (서울)에 서울 리뷰 1개가 표시되는지 확인
- [ ] `/city/2` (부산)에 부산 리뷰 1개가 표시되는지 확인
- [ ] `/city/3` (제주)에 제주 리뷰 1개가 표시되는지 확인
- [ ] 리뷰가 없는 도시(예: `/city/4` 대전)에 "아직 리뷰가 없습니다" 표시 확인
- [ ] 리뷰 카드에 아바타, 평점, 체류기간, 장단점 태그가 정확히 표시되는지 확인
- [ ] `npm run lint && npx tsc --noEmit` 통과

---

## [ ] Phase 8: 도시 카드 좋아요/싫어요 버튼 레이아웃 변경

### 오버뷰

도시 카드 하단의 좋아요/싫어요 버튼을 오른쪽 정렬에서 가운데 정렬로 변경하고, 버튼 배치를 `[👍 아이콘][좋아요 숫자][싫어요 숫자][👎 아이콘]` 형태로 재배치하여 좌우 대칭 레이아웃을 적용한다.

### 수정/개선 항목

- [ ] `city-card.tsx` — 좋아요/싫어요 컨테이너의 `justify-end`를 `justify-center`로 변경
- [ ] `city-card.tsx` — 버튼 내부 요소 순서 변경: 좋아요 버튼은 `[👍][숫자]`, 싫어요 버튼은 `[숫자][👎]` 순서로 배치하여 숫자가 가운데로 모이는 대칭 레이아웃 적용

### 검증 항목

- [ ] 좋아요/싫어요 버튼이 카드 하단 가운데에 정렬되는지 확인
- [ ] 버튼 배치가 `[👍][좋아요 수][싫어요 수][👎]` 순서인지 확인
- [ ] 좋아요/싫어요 클릭 인터랙션이 기존과 동일하게 동작하는지 확인
- [ ] 모바일/태블릿/데스크톱에서 레이아웃이 깨지지 않는지 확인
- [ ] `npm run lint && npx tsc --noEmit` 통과

---

## [ ] Phase 9: 필터바 Select 전체 너비 + 라벨 텍스트 추가

### 오버뷰

필터바의 4개 Select 드롭다운이 현재 고정 너비(`w-[120~140px]`)로 되어 있어 공간을 충분히 활용하지 못한다. 각 Select가 균등하게 최대 너비를 차지하도록 변경하고, Select 위에 "예산", "지역", "환경", "최고 계절" 라벨 텍스트를 표시한다.

### 수정/개선 항목

- [ ] `filter-bar.tsx` — 필터 컨테이너를 `flex flex-wrap`에서 `grid grid-cols-2 md:grid-cols-4`로 변경하여 각 Select가 균등 너비를 차지하도록 수정
- [ ] `filter-bar.tsx` — 각 Select 위에 라벨 텍스트("예산", "지역", "환경", "최고 계절") 추가
- [ ] `filter-bar.tsx` — SelectTrigger의 고정 너비(`w-[120~140px]`)를 `w-full`로 변경

### 검증 항목

- [ ] 4개 Select가 균등하게 전체 너비를 차지하는지 확인
- [ ] 각 Select 위에 라벨 텍스트가 표시되는지 확인
- [ ] 모바일에서 2열, 데스크톱에서 4열 그리드로 표시되는지 확인
- [ ] 필터 선택/해제 기능이 기존과 동일하게 동작하는지 확인
- [ ] `npm run lint && npx tsc --noEmit` 통과
